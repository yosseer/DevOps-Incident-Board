apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: beeper
  labels:
    app: postgresql
type: Opaque
stringData:
  # Database admin user
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: changeme123!
  # Application database user (used by backend)
  DB_USER: beeper
  DB_PASSWORD: beeper_secure_password_123
